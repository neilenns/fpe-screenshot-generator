{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch fpe.html in Edge",
      "type": "pwa-msedge",
      "request": "launch",
      "url": "http://localhost:8000/fpe.html",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Start Python HTTP server"
    }
  ]
}
